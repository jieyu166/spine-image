# Spine Image Project — 交班檔 (2026-06-11)

承接 `HANDOFF_2026-06-06.md`。本日主軸：**純推論端優化鏈**（不重訓主模型）把 overall_mean 從 144px 一路壓到 **97.6px**，median 從 79 → **41.9px**，首度跌破 100px。

## 速覽 — Session 結束狀態

- **Repo**: `https://github.com/jieyu166/spine-image`
- **主分支**: `main` @ `e30d67b`
- **生產配置**: **baseline 主模型 + fold3 ensemble + 全推論後處理鏈**
- **指標**: overall_mean **97.6px**、median **41.9px**、29/32 影像 < 100px

## 本日優化鏈（每一步都有實測對照，n=32 GT 影像）

| 步驟 | 動作 | overall | median | <100 | Commit |
|---|---|---|---|---|---|
| 起點 | V3.4 + aspect-fix v2 | 144.0 | 79.0 | 25/32 | (06-06) |
| 命名錨定 | L-spine 從 S1 往上錨定（C 從 C2） | 131.6 | 79.0 | 26/32 | `1252de3` |
| TTA | 旋轉±6°+亮度，機率空間平均，count 解耦 | 128.0 | 71.9 | 26/32 | `2e33658` |
| **DARK decode** | 取峰前 Gaussian 平滑 (σ=6 對齊 label) | **103.5** | **45.7** | 26/32 | `370e74d` |
| reliability 旗標 | corner_min<0.5 → low_confidence | — | — | — | `277714b` |
| ensemble | baseline + fold3 (sidecar 自動載入) | **97.6** | **41.9** | **29/32** | `e30d67b` |

**DARK decode 是最大單一躍進**（128→103.5），ensemble 收尾跌破 100。

## 各優化的「為什麼有效」與關鍵陷阱

### 1. 命名錨定 (`1252de3`)
- 問題：`_decode_v3` 一律從 names[0]=T12 起算。影像沒 T12（頂端裁切）但 count 多 1 → 整條序列往下錯位 → catastrophe。
- 修：L-spine 從 S1（薦椎，必入鏡、解剖最穩定）往上錨定 `names[-count:]`；C-spine 從 C2 往下。
- 效果：18971571 系列 306→87 / 315→138，零回歸。
- 注意：81161252、21584353 命名修正後仍 700+px，因為那是**模型真實定位失敗**（heatmap 峰值散掉），非命名問題。

### 2. TTA (`2e33658`)
- 變體：identity + 旋轉±6° + 亮度×0.85/1.15（**不含水平翻轉**，lateral spine 前後有醫學意義）。
- 旋轉變體把輸出 heatmap 逆旋轉回正規座標再於機率空間平均。
- **關鍵陷阱**：初版把 count_logits 也平均 → 旋轉翻轉 predicted_count → anchor 命名亂跳 → 18971571-1 暴增 87→291。**改成 count 只取 identity 那次**才解決。
- 掃描發現：旋轉是主力，亮度幾乎沒貢獻。

### 3. DARK decode (`370e74d`) ★最重要
- 原理：128 heatmap 上 1px ≈ 原圖 24px，sub-pixel 精度影響極大。取 argmax 前先 Gaussian 平滑，把多模/雜訊的 argmax 正規化到真實高斯質心。
- σ=6 對齊訓練 label 的 sigma（DARK 理論：decode kernel 應匹配 label kernel）。
- 每個 corner 是獨立 channel，blur 不污染相鄰椎體；對稱高斯平滑不移動峰位 → 對 GT 距離單調下降是「更接近真值」的直接證明，非塌縮假象。
- 效果：23 張改善 >20px，**零回歸**。
- API：`inf.decode_blur_sigma`（預設 6.0，設 0 關閉）。

### 4. Ensemble (`e30d67b`)
- 5-fold V3.4 CV 訓練（fold val: 0.82/0.97/0.62/1.03/1.67，**高變異**）。
- **全 5-fold ensemble = 107.6px，反輸 baseline 103.5** — fold4/5 弱模型拖垮。
- **精選 ensemble**：只用品質相當的 baseline(0.6234) + fold3(0.6230) = **97.6px**。
- 關鍵：① ensemble 要「品質相當」才有用，含弱 fold 有害；② count head 必須取較準的 baseline 當主（fold3 當主退步到 107）。

## 生產配置（已就緒，零呼叫端改動）

推論端透過 **sidecar 自動載入** ensemble，所有呼叫端（compare / api_server / colab）不必改 code：

```
best_vertebra_model.pth            ← baseline 主模型 (val 0.6234)
best_vertebra_model.pth.ensemble   ← sidecar 純文字，內容: best_vertebra_model_fold3.pth
best_vertebra_model_fold3.pth      ← ensemble 成員 (val 0.6230)
```

`VertebraInference('best_vertebra_model.pth')` 會自動偵測 sidecar 並載入 fold3。

可調 API（在 instance 上設）：
```python
inf.tta = False              # 關 TTA
inf.decode_blur_sigma = 0    # 關 DARK decode
inf.aspect_min = 0.0         # 關 aspect-fix
inf.reliability_threshold = 0.5
# ensemble_paths=[...] 可明確指定，覆蓋 sidecar
```

## reliability 旗標 (`277714b`)
- 實測 corner_min（所有角點 confidence 最小值）與誤差相關 **-0.68**。
- predict() 回傳新增 `corner_confidence_min/avg`、`low_confidence`（門檻 0.5）。
- 2 張無解 catastrophe (81161252/21584353) 會被正確標記 low_confidence → 臨床 triage：提示「請完整複查」。

## ⚠️ 上線必帶檔案（Drive / Docker / Colab）

**三個檔缺一不可**，少了 sidecar 或 fold3 會 silently 退回 baseline 單模型（103.5px）：
1. `best_vertebra_model.pth`
2. `best_vertebra_model_fold3.pth`
3. `best_vertebra_model.pth.ensemble`

Colab 步驟見本檔末「Colab 部署」段。

## 檔案盤點（權重）

| 檔案 | 內容 | 生產 |
|---|---|---|
| `best_vertebra_model.pth` | baseline V3.4 (val 0.6234) | ✅ 主模型 |
| `best_vertebra_model_fold3.pth` | CV fold3 V3.4 (val 0.6230) | ✅ ensemble 成員 |
| `best_vertebra_model.pth.ensemble` | sidecar 清單 | ✅ |
| `best_vertebra_model_v34_baseline.pth` | baseline 備份（同主模型） | 備份 |
| `best_vertebra_model_fold{1,2,4,5}.pth` | CV 其餘 fold（弱，不用） | — |
| `best_vertebra_model_fold{N}_v32.pth` | 舊 V3.2 fold 備份 | — |
| `best_vertebra_model_retrain_0.6568.pth` | 06-10 單模型重訓（較差） | — |

## 已知無解長尾（需更多資料，非推論能修）

1. **81161252、21584353**（mean 730-780px）— 模型在難圖上 heatmap 峰值散掉的真實定位失敗。已被 reliability 旗標標記。
2. **嚴重側彎 81314815**、**滑椎 18971571** — 訓練集缺異常樣本。

排掉這 2 張 catastrophe 後，其餘 30 張 mean 僅 **52.7px** — 典型案例已相當準。

## 教訓（給下個 session）

- **35 樣本訓練變異極大**：同設定重訓 103.5↔133.5px、CV fold val 0.62~1.67。單純重訓不會穩定改善。
- **推論端優化 ROI 最高**：本日不重訓主模型，純後處理就從 144→97.6px。
- **ensemble 要精選**：品質相當才有用；高變異 CV 的弱 fold 是毒藥。
- **根本解仍是資料**：要再進一步（攻 catastrophe / 側彎 / 滑椎），需補標 hard case 到 60-80 張。

## 環境（不變）

```bash
pip install -r requirements.txt
pip install albumentations==1.3.1
# RadImageNet 權重放 pretrained/RadImageNet-ResNet50.pt（找不到 fallback ImageNet）
```
硬體：AMD CPU + nVidia 4060，cuda 可用。

## 重訓注意（若下次要再跑 5-fold）

- 先停睡眠：`powercfg /change standby-timeout-ac 0`（本日訓練曾被深夜睡眠中斷一次）。
- `python train_vertebra_model_cv.py --n-folds 5 --epochs 30 --unfreeze-epoch 5`
- CV 最後一個 fold 會把 `best_vertebra_model.pth` rename 成 fold5 → 訓練後記得從備份還原主模型。

---

## Colab 部署步驟

### 步驟 1：上傳 3 個檔到 Google Drive

把以下檔案上傳到 `MyDrive/Spine/weights/`（用瀏覽器拖曳或 rclone）：

```
best_vertebra_model.pth            (427 MB)
best_vertebra_model_fold3.pth      (427 MB)
best_vertebra_model.pth.ensemble   (純文字小檔)
```

> sidecar 內容只有一行：`best_vertebra_model_fold3.pth`
> 它用相對路徑，會自動在主模型同資料夾找 fold3，所以 3 個檔要放同一層。

### 步驟 2：開 Colab notebook

```
https://colab.research.google.com/github/jieyu166/spine-image/blob/main/spine_inference_colab.ipynb
```

### 步驟 3：切 GPU runtime

`Runtime → Change runtime type → T4 GPU → Save`

### 步驟 4：依序執行 Cell

| Cell | 動作 | 預期 |
|---|---|---|
| Cell 2 | mount Drive | 授權登入 |
| Cell 4 | clone `main` 分支 + pip install | ~30 秒 |
| Cell 5+ | 載入模型 + 啟動 Gradio | 印 `Model Vv3.4 loaded` → **應接著印 `[ensemble] 自動載入 sidecar` + `ensemble member: ...fold3`** → 最後 `Running on public URL: https://xxx.gradio.live` |

**驗證 ensemble 有生效**：Cell 5 載入時若有看到 `Ensemble 啟用：主模型 + 1 個成員` 才表示 fold3 有載到（97.6px）。若沒看到，代表 sidecar 或 fold3 沒上傳成功，會退回 baseline 單模型（103.5px）。

### 出錯排查

| 症狀 | 原因 | 解法 |
|---|---|---|
| 沒印 `[ensemble]` | sidecar 或 fold3 沒上傳 | 確認 3 個檔都在 `MyDrive/Spine/weights/` |
| `找不到權重檔` | 主模型路徑錯 | 確認 `best_vertebra_model.pth` 在 Drive |
| 推論慢 | 沒切 GPU | 重做步驟 3 |
| ensemble 推論變慢 | 正常 | 2 模型 × TTA 5 變體 = 10 次 forward/張，比單模型慢 ~2x |

---

最後更新：2026-06-11 by Claude (sandbox session)

# Spine Image Project — 交班檔 (2026-06-06)

延續 `HANDOFF_2026-06-05.md`。本日 session 主軸：V3.4 上線、修「椎體被預測成垂直長條」副作用、嘗試 V3.5 / V3.6 架構改動失敗、最後落地 aspect-fix post-processor 並完成驗證。

## 速覽 — Session 結束狀態

- **Repo**: `https://github.com/jieyu166/spine-image`
- **主分支**: `main` @ `ee9cb5c`（aspect-fix v2 已驗證）
- **生產 baseline**: **V3.4 (aspect-aware) + aspect-fix v2**
- **目前 `best_vertebra_model.pth`**: **V3.4 重訓版 val_loss 0.6234 / epoch 10**

## 最終實測（V3.4 retrain + aspect-fix v2）

| 指標 | V3.4 raw | V3.4 + aspect-fix v1 | V3.4 + aspect-fix v2 |
|---|---|---|---|
| overall_mean | 147.2 px | 145.9 px | **144.0 px** |
| median mean | 85.5 | 79.0 | **79.0** |
| p75 mean | 125.1 | 101.9 | **99.5** |
| < 100 px | 21/32 (66%) | 24/32 (75%) | **25/32 (78%)** |
| < 50 px | 3/32 | 2/32 | 2/32 |

aspect-fix 同時改善「垂直長條」cosmetic 問題與量化指標。

## 本日完整路線

| 階段 | 動作 | 結果 | Commit |
|---|---|---|---|
| Revert V3.3 | 把 6489378 V3.3 改動 revert 回 V3.2 | val_loss 2.22 → 1.36 baseline | `ecd7738` |
| Dataset 修 | image_path 解析 + 找不到樣本預先 drop | 46→35 train、12→9 val | `8963b99` |
| 重生 split | `regenerate_splits.py` 從 Images/ 重建 train/val | 補進 11098781 等 7 張漏標 case | `9592e47` |
| V3.4 | keep-aspect resize + PadIfNeeded 取代 squash | val_loss 0.6932、overall_mean 147 px | `159b431` |
| V3.5 | CenterNet 架構（1 center + 8 offset）三輪嘗試 | 全失敗 (overall_mean 321-374 px) | `claude/v3.5-center-offset` |
| V3.6 | V3.4 + soft-argmax geometric consistency loss | 失敗 (508 px) | `claude/v3.6-geom-consistency` |
| aspect-fix v1 | 固定 target=1.2 拉 X 軸 | overall_mean 145.9 px，部分過度修正 | `96b47c4` |
| **aspect-fix v2** | min_aspect=0.9 門檻 + max_scale=1.6 上限 | **overall_mean 144.0 px** ✓ | `ee9cb5c` |

## V3.x 系列性能對照（最終）

| 版本 | val_loss | overall_mean | median | < 100 px |
|---|---|---|---|---|
| V3.3 broken | 2.22 | 185 px | 175 | 16% |
| V3.2 fold2 (4/21) | 0.998 | 180 px | 88 | 64% |
| V3.2 NEW (35 split) | 0.752 | 185 px | 80 | 70% |
| V3.4 aspect-aware | 0.693 | 147 px | 86 | 66% |
| V3.5 sigma=4 | 1.88 | 359 px | 315 | 3% |
| V3.5.1 sigma=2+NMS5 | 2.20 | 374 px | 349 | 0% |
| V3.5.2 region 5×5 | 2.62 | 321 px | 319 | 3% |
| V3.6 geom loss | 3.00 | 508 px | 478 | 0% |
| V3.4 retrain | **0.6234** | 147 px | 86 | 66% |
| **V3.4 retrain + aspect-fix v2** | — | **144 px** | **79** | **78%** ⬅ 上線 |

## 本 session 完整路線

| 階段 | 動作 | 結果 | Commit |
|---|---|---|---|
| Revert V3.3 | 把 6489378 V3.3 改動 revert 回 V3.2 | val_loss 2.22 → 1.36 baseline | `ecd7738` |
| Dataset 修 | image_path 解析 + 找不到樣本預先 drop | 46→35 train、12→9 val（含 V3.2 fold weights 還可用）| `8963b99` |
| 重生 split | `regenerate_splits.py` 從 Images/ 重建 train/val | 補進 11098781 等 7 張漏標 case，35/9 樣本 | `9592e47` |
| V3.4 | keep-aspect resize + PadIfNeeded 取代 squash | val_loss 0.6932、overall_mean 147 px、70% < 100 px ⬅ **最佳** | `159b431` |
| V3.5 | CenterNet 架構（1 center + 8 offset）| 三輪嘗試（sigma 4→2、NMS 3→5、5×5 regional supervision）overall_mean 321-374 px | 在 `claude/v3.5-center-offset` 分支 |
| V3.6 | V3.4 + soft-argmax geometric consistency loss | overall_mean 508 px、aspect 0.10~0.34（更糟）| 在 `claude/v3.6-geom-consistency` 分支 |
| aspect-fix | 推論後處理：aspect < 0.7 時 X 軸圍中心放大到 1.2×height | 已 cherry-pick 到 main，等 V3.4 weights 配合 | `96b47c4` |

## V3.x 系列性能對照

| 版本 | val_loss | overall_mean | median mean | < 100 px | aspect (GT ~1.2) |
|---|---|---|---|---|---|
| V3.3 broken | 2.22 | 185 px | 175 px | 16% | — |
| V3.2 fold2 (4/21) | 0.998 | 180 px | 88 px | 64% | — |
| V3.2 NEW (35 split) | 0.752 | 185 px | 80 px | 70% | — |
| **V3.4 aspect-aware** | **0.693** | **147 px** | **86 px** | **66%** | **0.58 ❌** |
| V3.5 sigma=4 | 1.88 | 359 px | 315 px | 3% | 0.50-0.70 |
| V3.5.1 sigma=2+NMS5 | 2.20 | 374 px | 349 px | 0% | 0.50-0.70 |
| V3.5.2 region 5×5 | 2.62 | 321 px | 319 px | 3% | 0.46-0.68 |
| V3.6 geom loss | 3.00 | 508 px | 478 px | 0% | 0.10-0.34 |

## 為什麼 V3.5 / V3.6 都失敗

### V3.5 (CenterNet style)
35 樣本 + 統一 offset head 的最佳解是「學一個 60% size 的均值椎體模板套到所有椎體」。沒壓力學 size variation（所有椎體在 heatmap 上 size 都接近）。三輪嘗試診斷：
- V3.5 sigma=4：相鄰椎體 gaussian 完全融合，center map 變成「整段脊柱都是 center」一條長帶
- V3.5.1 sigma=2 + NMS 5×5：center map 修好，能找到 7 個離散 peak，但 offset 仍學成固定 (±6, ±5) pattern
- V3.5.2 region 5×5：對 GT center 周圍 25 個 pixel supervise（25× supervision points），offset 仍未學會椎體 size variation

### V3.6 (geometric consistency loss)
soft-argmax 在訓練早期（heatmap 噪聲多時）給出混亂的 corner 位置 → geom_loss 提供的梯度方向錯誤 → 干擾 heatmap 收斂。geom_weight=0.5 在 init 階段 geom=50 vs HM=12 太強。HM val 0.99 vs V3.4 ~0.65，heatmap 學不好導致整體位置爛。

### 結論
V3.4 的 32-channel 獨立 corner heatmap + channel embedding 在 35 樣本規模下是定位精度最高的。「垂直長條」副作用無法在不超過 35 樣本的條件下用架構/loss 改正。改用推論後處理 hard fix。

## aspect-fix v2 post-processor 規格（已驗證）

位置：`inference_vertebra.py.predict()` 中 `_decode_v3` 之後呼叫 `_apply_aspect_fix`

算法（門檻邏輯，非固定 target）：
- 對 4 角點完整的椎體（boundary 椎體 T12/S1 跳過）
- `aspect = (max(xs) - min(xs)) / (max(ys) - min(ys))`
- 若 `aspect < min_aspect`（預設 0.9）：
  - `target_W = min_aspect * H`（**只拉到剛好達門檻**）
  - `scale_x = min(target_W / W, max_scale)`（**加上限**避免極端拉伸）
  - X 軸圍中心放大；Y 軸不動

開關（在 VertebraInference instance 上設）：
```python
inf = VertebraInference('best_vertebra_model.pth')
inf.aspect_min       = 0.9    # 觸發門檻 (0.0 永遠關掉)
inf.aspect_max_scale = 1.6    # X 軸 scale 上限
```

實測效果：
- overall_mean 147 → 144 px（-2%）
- 78% 影像 mean dist < 100 px（從 66%）
- PRED 椎體大多 aspect ≥ 0.9（不再是垂直長條 0.5）
- 過度修正 case 大幅減少（v1 的 16307800 L1 從 2.46 仍存在但與本 fix 無關）

## 檔案盤點

| 檔案 | 內容 | 大小 |
|---|---|---|
| `best_vertebra_model.pth` | **V3.4 retrain (val 0.6234) ⬅ 生產用** | 427 MB |
| `best_vertebra_model_v35_2_backup.pth` | V3.5 架構 weights（不相容 V3.4 inference）| 428 MB |
| `best_vertebra_model_fold1~5.pth` | V3.2 5-fold weights（4/21）| 250 MB each |
| `checkpoint_vertebra_epoch_200.pth` | V3.1 weights（2/18）| 146 MB |

## 影像資料狀態

- 標註正本：`C:\Users\jai16\OneDrive\00 放射科\0筆記\Spine\Images\` 共 47 對 image+json
- 加上 root 4 個 spinefm 配對 = 51 candidate
- 扣除 7 個 .dcm-only 無 vertebrae 的殼 = 44 valid
- `regenerate_splits.py` seed=42 切 8:2 = 35 train / 9 val
- 11815585.png 在 session 中觀察期間消失（OneDrive 同步），目前 dataset 自動 drop 變 34 train

## 分支現況

| 分支 | 狀態 | 用途 |
|---|---|---|
| `main` | V3.4 + aspect-fix | 生產用 |
| `claude/v3.4-aspect-aware` | aspect-aware 開發 | 已 merge，保留紀錄 |
| `claude/v3.5-center-offset` | V3.5 → V3.5.1 → V3.5.2 連續實驗 | 失敗紀錄，不 merge |
| `claude/v3.6-geom-consistency` | V3.6 + aspect-fix 原始版本 | 失敗紀錄 + aspect-fix 出生地，不 merge |
| `claude/radimagenet-backbone-J9sjR` | V3.0~V3.3 開發 | 已部分 merge，保留紀錄 |

## 未解問題（長尾）

1. **Count head off-by-1**（81161252、21584353 系列）— gt=6/pred=7，後續 corner 序列全偏，aspect-fix 無法救
2. **嚴重側彎**（81314815）— 訓練集側彎樣本不足，模型把曲線拉直預測
3. **滑椎下半段**（18971571）— L4-L5-S1 滑椎讓下半段幾何混亂
4. **「垂直長條」aspect bug** — 已用 post-processor 治標，根治需要更多資料或不同架構

## 環境設定（不變）

```bash
pip install -r requirements.txt
pip install albumentations==1.3.1  # 1.4+ 在 Python 3.14 有 stringzilla 安裝問題
# RadImageNet 權重: 放到 pretrained/RadImageNet-ResNet50.pt
# 找不到會 fallback 到 torchvision ImageNet（會印警告）
```

硬體：AMD CPU + nVidia 4060，cuda 可用。

## 使用者習慣（不變）

- 中文 commit message / docstring / comment
- 絕不破壞穩定部分（標註工具 HTML、API server、Docker 部署）
- 每個重要改動獨立 commit + 解釋「為什麼這樣改」
- 訓練資料量：~35 張左右人工標註（短期不會加）

## 給下一個 session 的建議

**生產 baseline 已就緒**：V3.4 retrain (val 0.6234) + aspect-fix v2，overall_mean 144 px、78% < 100 px。

短期動作：
1. 上傳 `best_vertebra_model.pth` 到 Drive `MyDrive/Spine/weights/`
2. Colab notebook 自動接上新 weights（main 分支 inference 端已含 aspect-fix）
3. API server / Docker 重啟拿新 weights

長尾改進方向（依優先順序）：
1. **Count head off-by-1**：81161252、21584353 系列 catastrophic (mean 700-800 px)。aspect-fix 救不了。可嘗試 ordinal regression count head（cumulative sigmoid）。
2. **嚴重側彎 / 滑椎**：18971571 系列 (mean 305) 跟 81314815 (mean 197)。需更多異常樣本，或在 data aug 加 elastic transform。
3. **架構嘗試**：資料量到 80+ 樣本時可重啟 V3.5 CenterNet（V3.5 失敗主因是 35 樣本不夠學 offset variation）。

關鍵失敗紀錄（讓下個 session 別重蹈覆轍）：
- V3.5 CenterNet（1 center + 8 offset）三輪 sigma/NMS/region 調整皆失敗
- V3.6 geometric consistency loss（soft-argmax + relative shape MSE）失敗

---

最後更新：2026-06-06 by Claude (sandbox session)

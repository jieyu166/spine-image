# Spine Vertebra Detection - Docker 部署依賴
# CPU-only PyTorch (節省空間 ~800MB vs GPU ~2.5GB)
torch==2.6.0+cpu
torchvision==0.21.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Web framework
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart>=0.0.6

# Image processing
opencv-python-headless==4.10.*
numpy>=1.24,<2.0
Pillow>=10.0

# Medical imaging
pydicom>=2.4

# Data augmentation (訓練時需要，推理時 import train_vertebra_model 會用到)
albumentations>=1.3

# Visualization (inference_vertebra.py 需要)
matplotlib>=3.7

# Progress bar (train_vertebra_model.py 需要)
tqdm>=4.65

# Spine Image Project — 交班檔 (2026-06-05)

繼續 session 用。這份文件包含目前 V3.2/V3.3 模型開發、Colab 部署、QA 工具的全部狀態與待決定的下一步。

## 速覽

- **Repo**: `https://github.com/jieyu166/spine-image`
- **目前工作分支**: `claude/radimagenet-backbone-J9sjR`
- **最新 commit**: `a7b9773`（Windows 中文路徑修正）
- **本地工作目錄**: Windows local 對應到 repo root
- **使用者**: Jieyu，奇美放射科。Linux sandbox + Windows local 協作（code 改沙盒 → push 分支 → Windows pull + 訓練 / 推論）

## 目前的核心問題（待解決）

**V3.3 模型有嚴重的 channel mode collapse**：32 個 output channel 中多個塌縮到同一個 (x, y)，導致 anterior 側多數椎體角點全部擠在影像左下角附近 (~310, 2280)。

### 證據

`compare_annotations.py` 跑 40 張影像結果：
- mean distance **185 px**，max **1086 px**
- 整體 val_loss **2.22**（V3.2 cv mean 是 1.36 — V3.3 反而退步）
- Overlay 圖：綠點（GT）完美貼合椎體角點；紅點（模型）成扇形塌縮到少數幾個位置

最壞範例 `11098781.png`:
| 椎體 | corner | GT | Pred | distance |
|---|---|---|---|---|
| L1 | anteriorSuperior | (298, 679) | (664, 658) | 367 px ←模型放到 posterior 側 |
| L2 | anteriorInferior | (247, 1179) | (311, **2263**) | 1086 px ←塌縮到圖底 |
| L3 | anteriorSuperior | (239, 1313) | (309, **2297**) | 987 px ←同上 |
| L4 | anteriorSuperior | (251, 1651) | (313, **2309**) | 661 px ←同上 |
| ...同樣的 (310, 2280) 重複出現 5+ 次 ... |

### 診斷

V3.3 改動：
1. HEATMAP_SIZE 128 → 256
2. sigma 6 → 3（正樣本面積縮 16 倍）
3. Decoder 加 up1 stage（多 ~300K params）

問題鏈：
- sigma=3 + 32 channel 鑑別任務 → 每個 channel 收到的梯度信號太弱
- V3.1 加的 Channel Embedding 機制在 V3.2 還能撐住 channel 區分，到 V3.3 撐不住
- 多個 channel 收斂到同一個「好認」peak（同一個高對比邊界、同一個影像角落）

額外因素 — **aspect ratio 失真**：
- 使用者影像典型尺寸 1318×3082（縱橫比 2.34:1）
- 訓練/推論都 resize 到 512×512 方形 → 垂直方向被擠壓 2.34 倍
- V3.3 sigma=3 在 256 heatmap 上 → 回推原圖垂直方向僅 ~28 px = 不到一節椎體
- V3.2 sigma=6 在 128 上 → 原圖 ~56 px = 剛好一節椎體
- 結論：sigma 縮小 + 影像被垂直擠壓 = V3.3 的高斯 footprint 對這份資料根本不夠大

## 三個可選路徑（讓使用者決定）

### 方案 1：使用 V3.2 舊權重檔（如果還留著）
- 最快。重訓不需要
- 動作：確認本機是否還有 V3.2 訓練的 `.pth`（上次 "Training complete! Best val loss: 1.4854"）
- 如果有，直接拿來用，跳過下面方案

### 方案 2：Revert V3.3 → 回 V3.2 → 重訓
- 把 `train_vertebra_model.py` 的三個 V3.3 改動 revert
- 重訓一次，期望 val_loss 回到 1.3-1.5 範圍
- 預估 30-60 分鐘訓練時間

### 方案 3：V3.3 中間方案（要實驗）
- 保留 HEATMAP_SIZE=256、up1 stage
- sigma 改成 5（不要 3）
- 改用 keep-aspect padding 取代 squash resize（解決 aspect ratio 失真）
- 風險：可能還是 collapse；要花一輪重訓嘗試
- 收益：理論上能拿到比 V3.2 更高的定位精度

**目前使用者尚未選擇。** 等待回應後執行。

## 已完成的工作（本 session）

### 程式碼改動

| Commit | 內容 | 狀態 |
|---|---|---|
| `a7b9773` | 修 Windows 中文路徑 cv2 fail（np.fromfile + imdecode 替代）| ✅ 必留 |
| `d484145` | 新增 `compare_annotations.py` QA 工具 | ✅ 必留 |
| `310d4a3` | Colab notebook 改寫為 Gradio + Drive mount | ✅ 必留 |
| `6489378` | V3.3: HEATMAP_SIZE/sigma/up1 改動 | ❌ 可能要 revert（看方案 2/3 選哪個）|

### 新增/修改的檔案

```
spine_inference_colab.ipynb    Colab Gradio 推論介面（剪貼簿貼圖 + Drive mount）
compare_annotations.py          QA 工具（手標 vs 模型比較，不動原檔）
train_vertebra_model.py         V3.3 訓練腳本（HEATMAP_SIZE=256 / sigma=3 / up1 / 6489378）
inference_vertebra.py           推論（已修 Windows 路徑）
```

## QA 工具 (`compare_annotations.py`) 用法

```bash
python compare_annotations.py ^
  --input-dir "C:\Users\jai16\OneDrive\00 放射科\0筆記\Spine\Images" ^
  --output-dir ".\comparison_output" ^
  --spine-type L
```

輸出：
- `compare_summary.json` — 全資料夾按 max_distance 排序
- `<stem>_compare.json` — per-image 每角點距離
- `<stem>_compare.png` — 視覺化 overlay（綠=手標、紅=模型、線色照距離分三段）
- `<stem>_model.json` — 沒手標的影像產出模型 JSON 當起點

**重要設計約束**：**絕不修改原始 JSON**（使用者強調過）。腳本只開原檔以 `'r'` 模式。

## Colab Notebook (`spine_inference_colab.ipynb`)

Gradio 架構，避開 NAS 部署的 ngrok / FastAPI 複雜性。

- 權重從 Google Drive `MyDrive/Spine/weights/best_vertebra_model.pth` mount
- 預設抓 `claude/radimagenet-backbone-J9sjR` 分支（Cell 2 可改 `main`）
- 公開網址走 `.gradio.live`（72hr 免費，不用 ngrok）
- 影像輸入支援：clipboard button（widget 內按鈕，不是全頁 Ctrl+V）/ 拖曳 / 上傳 / DICOM
- 輸出：標註影像 + JSON 座標 + 可下載 JSON

## 使用者習慣與約束

- 中文 commit message / 中文 docstring / 中文 comment
- **絕不破壞穩定部分**：標註工具 HTML、API server、Docker 部署
- 每個重要改動獨立 commit + 解釋「為什麼這樣改」
- 模型相關改動要先 smoke test（5 epoch）確認 loss 下降
- 對訓練圖跟 unseen 圖都要看推論效果
- 訓練資料量：50 張左右人工標註（短期不會加）

## 訓練腳本參考

```bash
# 標準訓練
python train_vertebra_model.py --epochs 15 --unfreeze-epoch 5

# 5-fold CV
python train_vertebra_model_cv.py --n-folds 5 --epochs 30

# Stage A 對照組（不解凍 backbone）
python train_vertebra_model.py --epochs 15 --unfreeze-epoch -1
```

## V3 系列演進對照

| 版本 | HEATMAP_SIZE | sigma | up1 | val_loss (cv mean) | 備註 |
|---|---|---|---|---|---|
| V3.0 | 128 | 6 | ✗ | - | 多通道 heatmap baseline |
| V3.1 | 128 | 6 | ✗ | - | + 凍結 + CoordConv + Channel Embed + RepeatDataset |
| V3.2 | 128 | 6 | ✗ | **1.36 ± 0.27** | + RadImageNet + X-ray aug + 兩階段 unfreeze ← **目前 working baseline** |
| V3.3 | 256 | 3 | ✓ | 2.22 | ❌ Channel mode collapse |

## V3.3 → V3.2 Revert 對應位置

如果走方案 2，要改 `train_vertebra_model.py`：

```python
HEATMAP_SIZE = 128                              # was 256
# 移除 self.up1 = nn.Sequential(...) 整段
# forward() 移除 d1_up / d1 計算，coord_conv 改吃 d2
sigma=6                                          # was 3，在 create_multi_channel_heatmap 呼叫
model_version = 'v3.2'                           # 兩處 checkpoint 都改
docstring 移除 V3.3 段落
```

## V3.3 → 方案 3（aspect-aware）對應位置

如果走方案 3：

- `train_vertebra_model.py`:
  - `create_multi_channel_heatmap` 呼叫 sigma 改 5（in `VertebraDataset.__getitem__`）
  - FocalLoss 註解更新

- `get_transforms()`:
  - `A.Resize(512, 512)` → `A.LongestMaxSize(max_size=512) + A.PadIfNeeded(min_height=512, min_width=512, border_mode=cv2.BORDER_CONSTANT, value=0)`
  - **注意**：keypoint 在 PadIfNeeded 後會有 offset，要驗證 Albumentations 是否自動修正

- `inference_vertebra.py`:
  - `predict()` 裡的 `cv2.resize(image, (512, 512))` 改成等比 resize + pad
  - 推論結果 unpad 回原圖時要扣掉 pad offset

這個方案技術風險最高，但可能是 long-term 正解。

## 環境設定

```bash
# 訓練/推論依賴
pip install -r requirements.txt
# 額外
pip install albumentations==1.3.1  # 1.4+ 在 Python 3.14 有 stringzilla 安裝問題

# 推論需要 RadImageNet 權重
# 下載: https://github.com/BMEII-AI/RadImageNet
# 放到: pretrained/RadImageNet-ResNet50.pt
# 找不到會 fallback 到 torchvision ImageNet（會印警告）
```

## 立即下一步

**等使用者回答：方案 1 / 2 / 3 哪個？**

回答後：
- 方案 1 → 確認 V3.2 weights 路徑就結束
- 方案 2 → revert commit 6489378，push，使用者重訓
- 方案 3 → 新分支 `claude/v3.4-aspect-aware`，實作 padding + sigma=5，使用者重訓

## 已知小問題（非阻擋）

- `cv2.imread`/`cv2.imwrite` 在 Windows 對非 ASCII 路徑 fail — **已修**（commit a7b9773）
- Albumentations `remove_invisible=False` 仍會丟 out-of-bounds keypoint — **已防禦**（commit 6c11381）
- RadImageNet 權重 `backbone.N.X` 格式不相容 torchvision — **已 remap**（commit f792352）

---

最後更新：2026-06-05 by Claude (sandbox session)

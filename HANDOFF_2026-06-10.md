# Spine Image Project — 交班檔 (2026-06-10)

延續 `HANDOFF_2026-06-06.md`。本日 session 主軸：**純 inference 端優化**（零重訓、零新資料），把 overall_mean 從 144px 壓到 103.5px、median 從 79px 壓到 45.7px。換到新模型(Fable 5)後重新檢視專案,發現先前指標被假象拉高,逐一拆解修正。

## 速覽 — Session 結束狀態

- **Repo**: `https://github.com/jieyu166/spine-image`
- **主分支**: `main` @ `4c6cc4d`
- **生產 baseline**: **V3.4 weights (val 0.6234) + 4 層 inference 後處理**
- **權重檔**: `best_vertebra_model.pth`（V3.4 retrain，未動）
- **本次全部是 `inference_vertebra.py` 改動,不需重訓**

## 關鍵發現:先前的「144px」是假象

把 32 張按 count 對錯分群:

| 群組 | 張數 | mean | median |
|---|---|---|---|
| count 正確 | 29 | 103px | 74px |
| count 錯誤 | 3 | 539px | 732px |

模型在典型案例其實不錯。144px 幾乎全被 2 張 catastrophe(81161252, 21584353)撐高。順著這條線拆解,做了 4 個推論端改動。

## 本日 4 個改動(全在 inference_vertebra.py)

| # | 改動 | overall_mean | median | commit |
|---|---|---|---|---|
| 0 | (起點) aspect-fix v2 | 144.0 | 79.0 | `ee9cb5c` |
| 1 | **anchor 命名** (L 從 S1、C 從 C2) | 131.6 | 79.0 | `1252de3` |
| 2 | **TTA** (旋轉+亮度,機率空間平均) | 128.0 | 71.9 | `2e33658` |
| 3 | **DARK decode** (取峰前 Gaussian σ=6) | **103.5** | **45.7** | `370e74d` |
| 4 | **reliability flag** (低信心旗標) | — | — | `4c6cc4d` |

**累計:overall 144 → 103.5px (-28%)、median 79 → 45.7px (-42%)**

### 1. Anchor 命名 (`1252de3`)
- 問題:`_decode_v3` 一律從 `names[0]=T12` 起算 → 影像無 T12(頂端被裁)但 count 多 1 時,整條序列往下錯位 → catastrophe
- 修法:依 spine_type 從穩定端錨定。L-spine 薦椎 S1 幾乎必入鏡 → `names[-count:]`;C-spine C2 齒突明顯 → `names[:count]`
- 新增 `ANCHOR_CONFIG`、`_assign_slot_names()`,可用 `inf.anchor_mode` 覆寫
- 18971571 系列 mean 306→87、315→138,零回歸

### 2. TTA (`2e33658`)
- identity + 旋轉 ±6° + 亮度 ×0.85/×1.15,旋轉變體把 heatmap 逆旋轉回正規座標後機率空間平均
- **關鍵:count 只取 identity 那次**(初版平均 count_logits 會翻轉 predicted_count → 擾動 anchor 命名 → 18971571-1 暴增到 291)
- sweep 證實旋轉是主力、亮度貢獻小;不含水平翻轉(lateral spine 前後方向有醫學意義)
- 新增 `inf.tta`(預設開)、`inf.tta_ops`

### 3. DARK decode (`370e74d`) — 本 session 最大勝利
- `extract_peaks_from_heatmap` 取 argmax 前先對單通道 heatmap 做 Gaussian 平滑
- 原理:128 heatmap 上 1px ≈ 原圖 24px,平滑把雜訊/多模 argmax 正規化到真實高斯質心
- σ=6 對齊訓練 label kernel(DARK 理論);每個 corner 獨立 channel,blur 不污染鄰椎;對稱高斯平滑不移峰位,只去噪
- **23 張改善 >20px、零回歸**,連無解 catastrophe 都微降 → distance-to-GT 單調下降是「更接近真值」的直接證明
- 新增 `inf.decode_blur_sigma`(預設 6.0,設 0 關閉)、`inf.decode_blur_ksize`

### 4. Reliability flag (`4c6cc4d`)
- 實測 corner_min(最低角點信心)與誤差相關 **-0.68**:好案例 ~0.67-0.69,catastrophe ~0.33-0.43
- `predict()` 輸出新增 `corner_confidence_min/avg`、`low_confidence`(bool,門檻 `inf.reliability_threshold` 預設 0.5)
- 2 張 catastrophe 正確被標記 LOW-CONF → 臨床 triage:哪些 auto-annotation 不可信、需完整複查

## 最終性能 (35-split QA, n=32)

| 指標 | 數值 |
|---|---|
| overall_mean | **103.5 px** |
| median | **45.7 px**(僅佔影像高 1.5%) |
| < 100px | 26/32 |
| 排掉 2 catastrophe | mean 59.7px |

overall 與 median 的差距(103.5 vs 59.7 排cata)**完全來自 2 張無解 catastrophe**(各 ~740px)。

## 新增的可調 inference 參數(都在 VertebraInference instance 上設)

```python
inf = VertebraInference('best_vertebra_model.pth')
inf.anchor_mode          = None    # None=依 spine_type 自動 / 'bottom' / 'top'
inf.tta                  = True    # TTA 開關
inf.tta_ops              = None    # 自訂變體清單
inf.decode_blur_sigma    = 6.0     # DARK 平滑 sigma (0=關閉)
inf.decode_blur_ksize    = 15
inf.aspect_min           = 0.9     # aspect-fix 門檻
inf.aspect_max_scale     = 1.6
inf.reliability_threshold = 0.5    # low_confidence 門檻
```

## 完整 inference pipeline 順序(predict 內)

1. keep-aspect resize + pad (V3.4 aspect-aware)
2. TTA forward (旋轉+亮度變體,機率空間平均;count 取 identity)
3. anchor 命名 (slot → 解剖名稱)
4. DARK decode (取峰前 Gaussian + Taylor sub-pixel)
5. aspect-fix (垂直長條 → 拉 X 軸到 aspect≥0.9)
6. reliability flag (corner_min 計算)

## 工具

- `eval_tta_sweep.py`(新增):載入模型一次、掃多組推論設定的快速 eval harness,複用 compare_annotations 的 GT 配對。調 TTA/DARK 參數時用這個,不用每次重跑慢的 compare。

## 比較輸出資料夾(本 session 產生)

| 資料夾 | 內容 |
|---|---|
| `comparison_v34_aspectfix2/` | 起點 (144px) |
| `comparison_v34_anchor/` | +anchor (131.6) |
| `comparison_v34_tta2/` | +TTA (128.0) |
| `comparison_v34_dark/` | +DARK (103.5) ← 最終 overlay 在此 |

## 未解問題(長尾,需資料或重訓)

1. **2 張 catastrophe**(81161252, 21584353)— 模型在難圖上真實定位失敗(heatmap 峰值散掉),已用 reliability flag 標記,但定位本身需更多訓練資料才能根治
2. **count off-by-1** — anchor 命名已大幅緩解(命名對齊),但 count head 本身仍偶爾數錯
3. V3.5/V3.6 架構嘗試已證明 35 樣本撐不起來(見 06-06 交班檔)

## 給下一個 session 的建議

**生產已就緒**:V3.4 + 4 層後處理,median 45.7px。要上線:
1. 上傳 `best_vertebra_model.pth` 到 Drive `MyDrive/Spine/weights/`
2. Colab/API 端拉 main(inference 後處理全含在 `inference_vertebra.py`)

進一步改進(依 ROI):
1. **5-fold V3.4 ensemble** — 需重訓 5 fold(各 ~20min),平均 heatmap 壓重尾。最可能進一步降 catastrophe
2. **針對性補標註** — 81161252/21584353 這種難圖(疑似偏斜/低對比)正是訓練集缺的;補 10-20 張同類最能根治
3. DARK σ 微調(sweep 顯示 σ=7 / k=17 可再降 ~2px,但 σ=6 對齊訓練較穩健)

不要再碰架構(V3.5/V3.6 已證實 35 樣本不可行)。

## 使用者習慣(不變)

- 中文 commit / docstring / comment,繁體台灣用語
- 絕不破壞穩定部分(標註工具 HTML、API server、Docker)
- 每個改動獨立 commit + 解釋為什麼
- 訓練資料 ~35 張(短期不會加)
- 硬體:AMD CPU + nVidia 4060,cuda 可用

---

最後更新:2026-06-10 by Claude (Fable 5)
